import 'package:flutter/material.dart';

class CompassSimpleWidget extends StatelessWidget {
  const CompassSimpleWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
